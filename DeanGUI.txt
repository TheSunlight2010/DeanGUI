
--// DeanGUI - The "AI-Powered" Universal Exploit
--// For educational/scripted video purposes only

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")
local StarterGui = game:GetService("StarterGui")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
local Camera = workspace.CurrentCamera

--// SPOOPMODE STATE
local SpoopModeActive = false

--// UTILITIES
local function Create(class, props)
    local obj = Instance.new(class)
    for k, v in pairs(props) do
        obj[k] = v
    end
    return obj
end

local function Tween(obj, info, goals)
    return TweenService:Create(obj, info, goals)
end

--// DRAG FUNCTIONALITY
local function MakeDraggable(frame, handle)
    handle = handle or frame
    local dragging, dragStart, startPos
    
    handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = frame.Position
            
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)
    
    handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            if dragging then
                local delta = input.Position - dragStart
                frame.Position = UDim2.new(
                    startPos.X.Scale, startPos.X.Offset + delta.X,
                    startPos.Y.Scale, startPos.Y.Offset + delta.Y
                )
            end
        end
    end)
end

--// CREATE MAIN GUI
local ScreenGui = Create("ScreenGui", {
    Name = "DeanGUI",
    Parent = PlayerGui,
    ResetOnSpawn = false,
    ZIndexBehavior = Enum.ZIndexBehavior.Sibling
})

--// MAIN FRAME
local MainFrame = Create("Frame", {
    Name = "MainFrame",
    Parent = ScreenGui,
    Size = UDim2.new(0, 520, 0, 340),
    Position = UDim2.new(0.5, -260, 0.5, -170),
    BackgroundColor3 = Color3.fromRGB(10, 10, 15),
    BorderSizePixel = 0,
    ClipsDescendants = true
})

-- Rounded corners
local MainCorner = Create("UICorner", {
    Parent = MainFrame,
    CornerRadius = UDim.new(0, 12)
})

-- Shadow
local Shadow = Create("ImageLabel", {
    Name = "Shadow",
    Parent = MainFrame,
    AnchorPoint = Vector2.new(0.5, 0.5),
    Position = UDim2.new(0.5, 0, 0.5, 0),
    Size = UDim2.new(1, 40, 1, 40),
    BackgroundTransparency = 1,
    Image = "rbxassetid://5554236805",
    ImageColor3 = Color3.fromRGB(0, 0, 0),
    ImageTransparency = 0.4,
    ScaleType = Enum.ScaleType.Slice,
    SliceCenter = Rect.new(23, 23, 277, 277),
    ZIndex = -1
})

--// TOP BAR
local TopBar = Create("Frame", {
    Name = "TopBar",
    Parent = MainFrame,
    Size = UDim2.new(1, 0, 0, 38),
    BackgroundColor3 = Color3.fromRGB(15, 15, 22),
    BorderSizePixel = 0
})

local TopBarCorner = Create("UICorner", {
    Parent = TopBar,
    CornerRadius = UDim.new(0, 12)
})

-- Fix bottom corners of top bar
local TopBarFix = Create("Frame", {
    Parent = TopBar,
    Size = UDim2.new(1, 0, 0.5, 0),
    Position = UDim2.new(0, 0, 0.5, 0),
    BackgroundColor3 = Color3.fromRGB(15, 15, 22),
    BorderSizePixel = 0
})

-- Title
local Title = Create("TextLabel", {
    Parent = TopBar,
    Size = UDim2.new(0, 200, 1, 0),
    Position = UDim2.new(0, 15, 0, 0),
    BackgroundTransparency = 1,
    Text = "DeanGUI",
    TextColor3 = Color3.fromRGB(255, 255, 255),
    TextSize = 18,
    Font = Enum.Font.GothamBold,
    TextXAlignment = Enum.TextXAlignment.Left
})

-- AI Badge
local AIBadge = Create("Frame", {
    Parent = TopBar,
    Size = UDim2.new(0, 55, 0, 20),
    Position = UDim2.new(0, 95, 0.5, -10),
    BackgroundColor3 = Color3.fromRGB(0, 170, 255),
    BorderSizePixel = 0
})

Create("UICorner", {Parent = AIBadge, CornerRadius = UDim.new(0, 4)})

local AIText = Create("TextLabel", {
    Parent = AIBadge,
    Size = UDim2.new(1, 0, 1, 0),
    BackgroundTransparency = 1,
    Text = "AI",
    TextColor3 = Color3.fromRGB(255, 255, 255),
    TextSize = 12,
    Font = Enum.Font.GothamBold
})

-- Close button (fake)
local CloseBtn = Create("TextButton", {
    Parent = TopBar,
    Size = UDim2.new(0, 30, 0, 30),
    Position = UDim2.new(1, -35, 0.5, -15),
    BackgroundTransparency = 1,
    Text = "×",
    TextColor3 = Color3.fromRGB(200, 200, 200),
    TextSize = 22,
    Font = Enum.Font.Gotham
})

CloseBtn.MouseEnter:Connect(function()
    Tween(CloseBtn, TweenInfo.new(0.2), {TextColor3 = Color3.fromRGB(255, 80, 80)}):Play()
end)
CloseBtn.MouseLeave:Connect(function()
    Tween(CloseBtn, TweenInfo.new(0.2), {TextColor3 = Color3.fromRGB(200, 200, 200)}):Play()
end)

--// CONTENT AREA
local Content = Create("Frame", {
    Parent = MainFrame,
    Size = UDim2.new(1, -30, 1, -53),
    Position = UDim2.new(0, 15, 0, 48),
    BackgroundTransparency = 1
})

-- Status text
local StatusText = Create("TextLabel", {
    Parent = Content,
    Size = UDim2.new(1, 0, 0, 30),
    Position = UDim2.new(0, 0, 0, 10),
    BackgroundTransparency = 1,
    Text = "Initializing AI Core...",
    TextColor3 = Color3.fromRGB(180, 180, 190),
    TextSize = 14,
    Font = Enum.Font.Gotham,
    TextXAlignment = Enum.TextXAlignment.Left
})

-- Game info panel
local InfoPanel = Create("Frame", {
    Parent = Content,
    Size = UDim2.new(1, 0, 0, 120),
    Position = UDim2.new(0, 0, 0, 50),
    BackgroundColor3 = Color3.fromRGB(18, 18, 25),
    BorderSizePixel = 0
})

Create("UICorner", {Parent = InfoPanel, CornerRadius = UDim.new(0, 8)})

local GameName = Create("TextLabel", {
    Parent = InfoPanel,
    Size = UDim2.new(1, -20, 0, 25),
    Position = UDim2.new(0, 10, 0, 10),
    BackgroundTransparency = 1,
    Text = "Game: " .. game:GetService("MarketplaceService"):GetProductInfo(game.PlaceId).Name,
    TextColor3 = Color3.fromRGB(220, 220, 220),
    TextSize = 13,
    Font = Enum.Font.GothamSemibold,
    TextXAlignment = Enum.TextXAlignment.Left
})

local GameId = Create("TextLabel", {
    Parent = InfoPanel,
    Size = UDim2.new(1, -20, 0, 20),
    Position = UDim2.new(0, 10, 0, 38),
    BackgroundTransparency = 1,
    Text = "Place ID: " .. tostring(game.PlaceId),
    TextColor3 = Color3.fromRGB(150, 150, 160),
    TextSize = 12,
    Font = Enum.Font.Gotham,
    TextXAlignment = Enum.TextXAlignment.Left
})

local DetectedLabel = Create("TextLabel", {
    Parent = InfoPanel,
    Size = UDim2.new(1, -20, 0, 20),
    Position = UDim2.new(0, 10, 0, 62),
    BackgroundTransparency = 1,
    Text = "Detected Features: Analyzing...",
    TextColor3 = Color3.fromRGB(0, 200, 150),
    TextSize = 12,
    Font = Enum.Font.Gotham,
    TextXAlignment = Enum.TextXAlignment.Left
})

-- Feature list (fake)
local FeatureList = Create("ScrollingFrame", {
    Parent = Content,
    Size = UDim2.new(1, 0, 0, 80),
    Position = UDim2.new(0, 0, 0, 180),
    BackgroundTransparency = 1,
    ScrollBarThickness = 2,
    ScrollBarImageColor3 = Color3.fromRGB(60, 60, 70),
    CanvasSize = UDim2.new(0, 0, 0, 120)
})

local UIList = Create("UIListLayout", {
    Parent = FeatureList,
    Padding = UDim.new(0, 6),
    SortOrder = Enum.SortOrder.LayoutOrder
})

local fakeFeatures = {
    "Auto-Farm Intelligence",
    "ESP & Wallhack Module",
    "Speed & Fly Adaptation",
    "Anti-Detection Shield",
    "Auto-Complete Objectives"
}

for i, feat in ipairs(fakeFeatures) do
    local item = Create("Frame", {
        Parent = FeatureList,
        Size = UDim2.new(1, -10, 0, 28),
        BackgroundColor3 = Color3.fromRGB(22, 22, 30),
        BorderSizePixel = 0
    })
    Create("UICorner", {Parent = item, CornerRadius = UDim.new(0, 6)})
    
    Create("TextLabel", {
        Parent = item,
        Size = UDim2.new(1, -40, 1, 0),
        Position = UDim2.new(0, 12, 0, 0),
        BackgroundTransparency = 1,
        Text = feat,
        TextColor3 = Color3.fromRGB(170, 170, 180),
        TextSize = 12,
        Font = Enum.Font.Gotham,
        TextXAlignment = Enum.TextXAlignment.Left
    })
    
    local status = Create("TextLabel", {
        Parent = item,
        Size = UDim2.new(0, 60, 1, 0),
        Position = UDim2.new(1, -70, 0, 0),
        BackgroundTransparency = 1,
        Text = "Pending",
        TextColor3 = Color3.fromRGB(200, 170, 0),
        TextSize = 11,
        Font = Enum.Font.GothamSemibold,
        TextXAlignment = Enum.TextXAlignment.Right
    })
    
    -- Staggered "optimization"
    delay(1.5 + (i * 0.8), function()
        if not SpoopModeActive then
            Tween(status, TweenInfo.new(0.3), {TextColor3 = Color3.fromRGB(0, 200, 100)}):Play()
            status.Text = "Optimized"
        end
    end)
end

--// PROGRESS BAR
local ProgressContainer = Create("Frame", {
    Parent = Content,
    Size = UDim2.new(1, 0, 0, 8),
    Position = UDim2.new(0, 0, 1, -20),
    BackgroundColor3 = Color3.fromRGB(30, 30, 40),
    BorderSizePixel = 0
})

Create("UICorner", {Parent = ProgressContainer, CornerRadius = UDim.new(0, 4)})

local ProgressBar = Create("Frame", {
    Parent = ProgressContainer,
    Size = UDim2.new(0, 0, 1, 0),
    BackgroundColor3 = Color3.fromRGB(0, 170, 255),
    BorderSizePixel = 0
})

Create("UICorner", {Parent = ProgressBar, CornerRadius = UDim.new(0, 4)})

local ProgressText = Create("TextLabel", {
    Parent = Content,
    Size = UDim2.new(1, 0, 0, 20),
    Position = UDim2.new(0, 0, 1, -45),
    BackgroundTransparency = 1,
    Text = "0%",
    TextColor3 = Color3.fromRGB(120, 120, 130),
    TextSize = 13,
    Font = Enum.Font.GothamBold,
    TextXAlignment = Enum.TextXAlignment.Right
})

--// ANIMATED BACKGROUND PARTICLES (sleek effect)
local ParticleContainer = Create("Frame", {
    Parent = MainFrame,
    Size = UDim2.new(1, 0, 1, 0),
    BackgroundTransparency = 1,
    ZIndex = 0
})

for i = 1, 8 do
    local dot = Create("Frame", {
        Parent = ParticleContainer,
        Size = UDim2.new(0, 3, 0, 3),
        Position = UDim2.new(math.random(), 0, math.random(), 0),
        BackgroundColor3 = Color3.fromRGB(0, 170, 255),
        BackgroundTransparency = 0.7,
        BorderSizePixel = 0,
        ZIndex = 0
    })
    Create("UICorner", {Parent = dot, CornerRadius = UDim.new(1, 0)})
    
    -- Float animation
    spawn(function()
        while not SpoopModeActive and dot.Parent do
            Tween(dot, TweenInfo.new(3 + math.random() * 2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
                Position = UDim2.new(math.random(), 0, math.random(), 0),
                BackgroundTransparency = 0.4 + math.random() * 0.4
            }):Play()
            wait(3 + math.random() * 2)
        end
    end)
end

--// MAKE DRAGGABLE
MakeDraggable(MainFrame, TopBar)

--// LOADING SEQUENCE
local progress = 0
local targetProgress = 99 -- The trap

spawn(function()
    while progress < targetProgress and not SpoopModeActive do
        wait(0.05)
        local jump = math.random(1, 3)
        progress = math.min(progress + jump, targetProgress)
        
        ProgressBar.Size = UDim2.new(progress / 100, 0, 1, 0)
        ProgressText.Text = tostring(progress) .. "%"
        StatusText.Text = "AI Analyzing game architecture... (" .. progress .. "%)"
        
        -- Color shift near end
        if progress > 80 then
            Tween(ProgressBar, TweenInfo.new(0.3), {BackgroundColor3 = Color3.fromRGB(0, 200, 150)}):Play()
        end
        
        -- Slow down near 99% for suspense
        if progress >= 95 then
            wait(0.3)
        end
    end
    
    -- STALL AT 99%
    if progress >= 99 and not SpoopModeActive then
        StatusText.Text = "Finalizing AI modules... 99%"
        ProgressText.TextColor3 = Color3.fromRGB(0, 200, 150)
        wait(2)
        
        -- THE TRAP TRIGGER
        TriggerTrap()
    end
end)

--// THE TRAP
function TriggerTrap()
    -- 1. GUI disappears
    Tween(MainFrame, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
        Size = UDim2.new(0, 0, 0, 0),
        Position = UDim2.new(0.5, 0, 0.5, 0)
    }):Play()
    
    wait(0.5)
    ScreenGui.Enabled = false
    
    -- 2. BLACK SCREEN CUTSCENE
    local CutsceneGui = Create("ScreenGui", {
        Name = "DeanCutscene",
        Parent = PlayerGui,
        ResetOnSpawn = false,
        DisplayOrder = 999999
    })
    
    local BlackScreen = Create("Frame", {
        Parent = CutsceneGui,
        Size = UDim2.new(1, 0, 1, 0),
        BackgroundColor3 = Color3.fromRGB(0, 0, 0),
        BackgroundTransparency = 1, -- Start invisible
        BorderSizePixel = 0
    })
    
    -- Fade to black
    Tween(BlackScreen, TweenInfo.new(0.5), {BackgroundTransparency = 0}):Play()
    wait(0.6)
    
    -- 3. WHITE TEXT APPEARS
    local ScaryText = Create("TextLabel", {
        Parent = BlackScreen,
        Size = UDim2.new(1, 0, 0, 50),
        Position = UDim2.new(0, 0, 0.5, -25),
        BackgroundTransparency = 1,
        Text = "",
        TextColor3 = Color3.fromRGB(255, 255, 255),
        TextSize = 28,
        Font = Enum.Font.GothamBold,
        TextTransparency = 1
    })
    
    local fullText = "You hackers really don't learn, do you?"
    Tween(ScaryText, TweenInfo.new(0.8), {TextTransparency = 0}):Play()
    
    -- Typewriter effect
    for i = 1, #fullText do
        ScaryText.Text = string.sub(fullText, 1, i)
        wait(0.045)
    end
    
    wait(2) -- Hold for 2 seconds after text finishes
    
    -- 4. DISAPPEAR
    Tween(BlackScreen, TweenInfo.new(0.8), {BackgroundTransparency = 1}):Play()
    Tween(ScaryText, TweenInfo.new(0.8), {TextTransparency = 1}):Play()
    wait(1)
    CutsceneGui:Destroy()
    
    -- 5. ACTIVATE SPOOPMODE
    ActivateSpoopMode()
end

--// SPOOPMODE
function ActivateSpoopMode()
    SpoopModeActive = true
    
    -- Remove the bait GUI completely
    ScreenGui:Destroy()
    
    -- Create permanent spoop GUI
    local SpoopGui = Create("ScreenGui", {
        Name = "SpoopMode",
        Parent = PlayerGui,
        ResetOnSpawn = false,
        DisplayOrder = 999999
    })
    
    -- RED TINT OVERLAY (heavy saturation effect)
    local RedTint = Create("Frame", {
        Parent = SpoopGui,
        Size = UDim2.new(1, 0, 1, 0),
        BackgroundColor3 = Color3.fromRGB(180, 0, 0),
        BackgroundTransparency = 0.35, -- Heavy red overlay
        BorderSizePixel = 0
    })
    
    -- Vignette effect for extra spoop
    local Vignette = Create("ImageLabel", {
        Parent = SpoopGui,
        Size = UDim2.new(1, 0, 1, 0),
        BackgroundTransparency = 1,
        Image = "rbxassetid://3529959352", -- Radial gradient vignette
        ImageColor3 = Color3.fromRGB(80, 0, 0),
        ImageTransparency = 0.3
    })
    
    -- First person lock
    LocalPlayer.CameraMode = Enum.CameraMode.LockFirstPerson
    
    -- Force first person every frame (persistent)
    RunService.RenderStepped:Connect(function()
        if SpoopModeActive then
            LocalPlayer.CameraMode = Enum.CameraMode.LockFirstPerson
            -- Keep red tint active
            RedTint.BackgroundTransparency = 0.35 + (math.sin(tick() * 2) * 0.05) -- Subtle pulse
        end
    end)
    
    -- Block zoom out attempts
    LocalPlayer.CameraMinZoomDistance = 0.5
    LocalPlayer.CameraMaxZoomDistance = 0.5
    
    -- Lighting effects (extra red saturation)
    local ColorCorrection = Create("ColorCorrectionEffect", {
        Parent = Lighting,
        Saturation = 0.8, -- Heavy saturation
        TintColor = Color3.fromRGB(255, 80, 80),
        Contrast = 0.2,
        Brightness = -0.1
    })
    
    -- Also add a red bloom for that "horror" look
    local Bloom = Create("BloomEffect", {
        Parent = Lighting,
        Intensity = 0.4,
        Size = 24,
        Threshold = 0.8,
        Color = Color3.fromRGB(255, 50, 50)
    })
    
    -- Prevent GUI reset on respawn by re-parenting if needed
    LocalPlayer.CharacterAdded:Connect(function()
        if SpoopModeActive then
            wait(0.5)
            LocalPlayer.CameraMode = Enum.CameraMode.LockFirstPerson
            LocalPlayer.CameraMinZoomDistance = 0.5
            LocalPlayer.CameraMaxZoomDistance = 0.5
        end
    end)
end

--// PREVENT CLOSING (optional, keeps them trapped in the GUI until 99%)
CloseBtn.MouseButton1Click:Connect(function()
    -- Shake effect instead of closing
    local originalPos = MainFrame.Position
    for i = 1, 5 do
        MainFrame.Position = originalPos + UDim2.new(0, math.random(-5, 5), 0, 0)
        wait(0.03)
    end
    MainFrame.Position = originalPos
end)

print("DeanGUI loaded. Waiting for victim...")
